extends layout

block content
  nav(class="navbar navbar-light navbar-expand-md")
    div(class="container-fluid")
      a(class="navbar-brand", href="#")
        strong SBR
      button(data-toggle="collapse", class="navbar-toggler", data-target="#navcol-1")
        span(class="sr-only") Toggle navigation
        span(class="navbar-toggler-icon")
      div(class="collapse navbar-collapse", id="navcol-1")
        ul(class="nav navbar-nav")
          li(class="nav-item", role="presentation")
            a(class="nav-link", href="login") Login
          li(class="nav-item", role="presentation")
            a(class="nav-link active", href="#") My Lists
          li(class="nav-item", role="presentation")
            a(class="nav-link", href="#") Create New List
          li(class="nav-item", role="presentation")

  div(class="container")
    form(method="GET" action="/api/test")
      label Email Address
      input.form-control(name="value" placeholder="SELECT", type="text", id="value")
      button(class="btn btn-primary", value="Submit", type="submit") Search Items in List  
